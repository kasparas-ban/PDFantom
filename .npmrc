# Vite+ 0.2.x implements Vite 8 but publishes under its own 0.x version,
# and its optional TypeScript peer range has not yet added TypeScript 7.
legacy-peer-deps=true

# Keep the lockfile portable across public, corporate, and mirror registries.
omit-lockfile-registry-resolved=true
